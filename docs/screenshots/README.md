# Screenshots

Drop dashboard PNG screenshots here so the main README renders them:

- `dashboard.png` — API inventory / overview
- `endpoints.png` — endpoint detail with p50/p95/p99 latency

Tip: capture at ~1400px wide for crisp rendering on GitHub. Screenshots are the
single biggest driver of clicks from search results and social posts.
